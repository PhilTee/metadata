<%= form_for(@facet) do |f| %>
  <% if @facet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facet.errors.count, "error") %> prohibited this facet from being saved:</h2>

      <ul>
      <% @facet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <table>
      <tr>
        <th><%= f.label :name %></th>
        <td><%= f.text_field :name %></td>
      </tr>
      <tr>
        <th><%= f.label :description %></th>
        <td><%= f.text_area :description %></td>
      </tr>
    </table>
    <%= f.submit %>
  </div>
<% end %>
